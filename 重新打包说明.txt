重新打包步骤：

1. 先构建项目
   npm run build

2. 如果构建成功，检查 dist/assets/ 下的 HomePage-*.js 文件时间戳是否更新

3. 部署时确保上传的是新生成的 dist 文件夹

4. 如果部署到服务器后仍无变化，可能是浏览器缓存，尝试：
   - 按 Ctrl+F5 强制刷新
   - 清除浏览器缓存
   - 检查 index.html 中引用的 JS 文件名是否变化（Vite 会添加 hash）